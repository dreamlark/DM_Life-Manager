# dm-life NAS 部署环境变量（all-in-one 单镜像）
# 用法：复制本文件为 .env，按需用强随机值替换 JWT_SECRET。
#
# 在 NAS 终端用以下命令生成：
#   openssl rand -base64 48
#
# 说明：
#   JWT_SECRET —— server 的 JWT 签名密钥，必须强随机、不要留空或提交到仓库。
#                 留空时 all-in-one 镜像会在首次启动自动生成并持久化到 /data/.env.auto（幂等）。
#
#   （已移除）ENGINE_API_TOKEN —— 联机版已收敛为单一后端 server，不再有独立 engine 进程，
#   故不再需要 engine/server 共享令牌。

JWT_SECRET=CHANGE_ME_use_openssl_rand_base64_48
